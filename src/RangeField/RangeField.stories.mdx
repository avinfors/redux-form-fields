import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { Field } from "redux-form";
import RangeField from "./RangeField";

<Meta title="RangeField" />

# RangeField

Using `RangeField` creates a range input.

<Canvas>
  <Story name="Simple">
    <Field
      component={RangeField}
      label="Simple range input"
      max={50}
      min={10}
      name="rangeField.simple"
      parse={(value) => (!isNaN(value) ? parseFloat(value) : value)}
      step={2.5}
    />
  </Story>
</Canvas>

## Props

<ArgsTable of={RangeField} />
